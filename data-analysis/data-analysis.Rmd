---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r}
library(mgcv)
library(visreg)
df <- read.csv("complete.csv")
b2 <- lm(dist_search ~ stage, df)
summary(b2)
visreg(b2)
```



```{r}
library(mgcv)
library(visreg)
df <- read.csv("data-form.csv")
df$type <- factor(df$type)
b2 <- gam(dist_search ~ s(stage, k = 4, fx = TRUE, by = type) + type, data = df, family = quasibinomial(link="logit"), method = "REML")
summary(b2)
#visreg(b2, xvar = "stage", by = "type", data = df2, method = "REML")
plot(b2, pages = 1, trans = plogis, shift = coef(b)[1], shade = TRUE, shade.col = "lightgreen", col= "purple")
plot(b2, shade = TRUE, pages = 1, scale = 0, seWithMean = TRUE)
```


```{r}
library(mgcv)
library(visreg)
df <- read.csv("data_only.csv")
df$type <- factor(df$type)
b2 <- gam(dist_search ~ s(stage, k = 4, fx = TRUE, by = type) + type, data = df, family = quasibinomial(link="logit"), method = "REML")
summary(b2)
#visreg(b2, xvar = "stage", by = "type", data = df2, method = "REML")
plot(b2, pages = 1, trans = plogis, shift = coef(b)[1], shade = TRUE, shade.col = "lightgreen", col= "purple")
plot(b2, shade = TRUE, pages = 1, scale = 0, seWithMean = TRUE)
```




```{r}
library(mgcv)
library(visreg)
df <- read.csv("scores.csv")
b2 <- gam(infovis ~ s(phase, k = 5, fx = TRUE), data = df, family = gaussian(), method = "REML")
summary(b2)
plot(b2, pages = 1, trans = plogis, shift = coef(b)[1], shade = TRUE, shade.col = "lightgreen", col= "purple")
plot(b2, shade = TRUE, pages = 1, scale = 0, seWithMean = TRUE)
```

```{r}
library(mgcv)
library(visreg)
df <- read.csv("scores.csv")
b2 <- gam(user_requirement ~ s(phase, k = 5, fx = TRUE), data = df, family = gaussian(), method = "REML")
summary(b2)
plot(b2, pages = 1, trans = plogis, shift = coef(b)[1], shade = TRUE, shade.col = "lightgreen", col= "purple")
plot(b2, shade = TRUE, pages = 1, scale = 0, seWithMean = TRUE)
```



```{r}
library(mgcv)
library(visreg)
df <- read.csv("scores.csv")
b2 <- gam(mashup ~ s(phase, k = 5, fx = TRUE), data = df, family = binomial(link="logit"), method = "REML")
summary(b2)
plot(b2, pages = 1, trans = plogis, shift = coef(b)[1], shade = TRUE, shade.col = "lightgreen", col= "purple")
plot(b2, shade = TRUE, pages = 1, scale = 0, seWithMean = TRUE)
```

```{r}
library(mgcv)
library(visreg)
df <- read.csv("scores.csv")
b2 <- gam(error_raw ~ s(phase, k = 5, fx = TRUE), data = df, family = betar(link="logit"), method = "REML")
summary(b2)
plot(b2, pages = 1, trans = plogis, shift = coef(b)[1], shade = TRUE, shade.col = "lightgreen", col= "purple")
plot(b2, shade = TRUE, pages = 1, scale = 0, seWithMean = TRUE)
```

