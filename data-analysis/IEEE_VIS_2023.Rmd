---
title: "Untitled"
output: html_document
---

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
df <- read.csv("cross_section.csv")
df
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
cor(df$dist_search_vis, df$dist_search_novel, method = "spearman")
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
cor(df$dist_search_vis, df$dist_search_data, method = "spearman")
```

```{r}
cor(df$dist_search_data, df$dist_search_novel, method = "spearman")
```

```{r}
library(mgcv)
library(betareg)
library(visreg)
library(modelsummary)
#df$stage <- factor(df$stage)
#df <- subset(df, type != "charts"&  type != "simple")
mod <- gam(dist_search_data ~ s(stage, k = 4, fx = TRUE), data = df, family = quasibinomial(link="logit"), method = "REML")
plot(mod,
     trans=plogis,
     shift=coef(mod)[1],
     main = "Beta GAM Shifted Intercept")
summary(mod)
```
```{r}
library(mgcv)
library(betareg)
library(visreg)
library(modelsummary)
#df$stage <- factor(df$stage)
#df <- subset(df, type != "charts"&  type != "simple")
mod <- gam(dist_search_novel ~ s(stage, k = 4, fx = TRUE), data = df, family = betar(link="logit"), method = "REML")
plot(mod,
     trans=plogis,
     shift=coef(mod)[1],
     main = "Beta GAM Shifted Intercept")
summary(mod)
```
```{r}
library(mgcv)
library(betareg)
library(visreg)
library(modelsummary)
#df$stage <- factor(df$stage)
#df <- subset(df, type != "charts"&  type != "simple")
mod <- gam(dist_search_vis ~ s(stage, k = 4, fx = TRUE), data = df, family = betar(link="logit"), method = "REML")
plot(mod,
     trans=plogis,
     shift=coef(mod)[1],
     main = "Beta GAM Shifted Intercept")
summary(mod)
```

